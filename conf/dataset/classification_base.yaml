# @package _global_

dataset:
  type: classification
  simple_cls:

  set_cls:
    n_way: ${exp.n_way}
    n_support: ${exp.n_shot}
    n_query: ${exp.n_query}

# What splits to run on evaluation
eval:
  eval_split: [train, val, test]
