name:
type: classification
subset: 1.0

simple_cls:
  subset: ${dataset.subset}

set_cls:
  n_way: ${n_way}
  n_support: ${n_shot}
  n_query: ${n_query}
  subset: ${dataset.subset}

loader:
  num_workers: 0 # Number of processes to use for data loader
  pin_memory: false # Use with GPU
